# momo · 设计文档

> 版本：v5.0（截至 2026-08-04）
> 形态：多文件 Web 应用（主应用零依赖、离线可用、数据本地留存 + Supabase 云端同步 + Edge Functions 云端 AI）
> 运行环境：桌面浏览器 + 手机浏览器（vivo X200 Pro Mini / 夸克浏览器验证），GitHub Pages 在线访问
> 文件：`index.html`（跳转页）+ `工作台.html`（主应用）+ `css/style.css` + `js/*.js`（9 模块）+ `manifest.json` + `sw.js` + `icon.jpg` / `icon.svg` + `supabase/functions/*`（Edge Functions）+ `.github/workflows/deploy-supabase.yml`
> 在线地址：https://ginwolley.github.io/mymomo/工作台.html

---

## 一、项目概述与目标

### 1.1 定位
一个**本地优先、离线可用、可跨端同步**的个人生活管理工具。覆盖「健身与身体管理」「生理期」「工资记录」「囤货管理」「密码管理」五大类长期记录需求。

### 1.2 核心目标
| 目标 | 说明 |
|---|---|
| 数据本地留存 | 全部数据存于浏览器 `localStorage`（键 `fit_workbench_v1`），不依赖任何服务器 |
| 手机可用 | 部署于 GitHub Pages，手机浏览器直接访问；不同 WiFi/流量均可用 |
| 多端同步 | 电脑 ↔ 手机通过 Supabase 自动同步（启动拉取 + 保存上传 + 手动按钮） |
| 素雅清爽 | 冷调浅蓝底色 + 扁平白卡片 + 亮青绿强调色，无霓虹渐变与拟物光效 |
| 可扩展 | 导航为多级树结构，新增板块只需在 `NAV_TREE` 增加节点 |
| 可备份 | JSON 导出/导入 + Supabase 云端备份，换设备/清缓存可恢复 |
| AI 能力（可选） | 囤货 AI 识图、AI 贴纸生成、条码查询全部走 Supabase Edge Functions，Key 在服务端不暴露 |

### 1.3 技术形态
- **多文件核心**：主应用 `工作台.html` 为 HTML 骨架，样式与逻辑按模块拆分到 `css/style.css` 与 `js/` 下 9 个文件，**无任何 CDN / 第三方库**（仅 Supabase JS SDK 外链）。
- **图表**：手写内联 SVG（折线、多线、坐标轴），不引外部绘图库。
- **存储**：`localStorage` 为主 + `seed-data.js` 种子兜底 + Supabase 云同步 + JSON 导入导出，四重保障。
- **部署**：GitHub Pages 静态托管前端；Supabase 托管数据与 Edge Functions；GitHub Actions 自动部署函数（push 即部署，无需手动）。
- **PWA**：`manifest.json` + `sw.js` 支持全屏与离线。

---

## 二、设计理念与视觉规范

### 2.1 风格定位
扁平、素雅、克制。冷调浅蓝底色 + 扁平白卡片 + 亮青绿强调色。大圆角（卡片 24px，按钮 16px），无边框阴影悬浮效果。

### 2.2 设计 Token（CSS 变量）
| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#E6F7FF` | 背景主色（冷调浅蓝） |
| `--bg-soft` | `#F2FBFF` | 次要背景 |
| `--glass` | `#FFFFFF` | 卡片底色（纯白） |
| `--glass-border` | `#EEF5F8` | 卡片描边 |
| `--ink` | `#1A1A1A` | 主文字 |
| `--ink-light` | `#666666` | 次要文字 |
| `--ink-muted` | `#999999` | 弱化文字 |
| `--gold` | `#36D1C9` | 亮青绿强调色（按钮/选中态） |
| `--gold-soft` | `#E0F9F6` | 青绿浅底 |
| `--gold-dark` | `#25B8B0` | 青绿深色文字 |
| `--rose` | `#FFAEC2` | 玫瑰色（生理期） |
| `--rose-soft` | `#FFF0F4` | 玫瑰色浅底 |
| `--sage` | `#36D1C9` | 青绿（健身/摄入） |
| `--sky` | `#7CC4E8` | 天蓝色 |
| `--money` | `#C9A227` | 金色（工资） |
| `--money-soft` | `#FDF5E0` | 金色浅底 |
| `--danger` | `#E08A8A` | 危险色 |
| `--period` | `var(--rose)` | 生理期强调色 |
| `--period-text` | `#D96A8C` | 生理期文字色 |
| `--card` | `#FFFFFF` | 日历卡片白色 |
| `--line` | `#E7F3F8` | 输入框边框 |
| `--radius` | `16px` | 按钮圆角 |
| `--radius-sm` | `12px` | 小圆角 |
| `--radius-lg` | `24px` | 大圆角（卡片） |
| `--shadow-soft` | `0 2px 14px rgba(60,140,160,.07)` | 卡片阴影 |
| `--shadow-lift` | `0 6px 24px rgba(30,90,110,.12)` | 悬浮阴影 |

### 2.3 排版与交互
- 系统无衬线字体栈：`"Noto Sans SC","PingFang SC","Microsoft YaHei",sans-serif`（无 Google Fonts 外链）
- 等宽数字：`"JetBrains Mono","Noto Sans SC",monospace`
- 全局 `overflow-wrap / word-break`，长文本不溢出裁切
- 输入聚焦字号 16px（防手机浏览器自动放大）
- 图标全部为极简线条 SVG（内联 `ICONS` 字典），无 emoji / 图形装饰
- 操作按钮（查看/删除/复制）使用简约线条 SVG 图标，无边框无底色

### 2.4 背景设计
body 纯色 `#E6F7FF`，与 `theme-color` 一致，确保手机状态栏无分界线。渐变效果仅作用于 `.content` 区域：
```
radial-gradient(circle at 12% 6%, rgba(124,196,232,.16) 0%, transparent 42%),
radial-gradient(circle at 88% 12%, rgba(54,209,201,.12) 0%, transparent 38%),
radial-gradient(circle at 75% 92%, rgba(124,196,232,.14) 0%, transparent 42%),
radial-gradient(circle at 18% 85%, rgba(54,209,201,.10) 0%, transparent 36%)
```

---

## 三、整体架构

### 3.1 文件结构（多文件模块化）
```
index.html        跳转页：location.replace 到 工作台.html（保留 query/hash），适配根路径访问
工作台.html       主应用：HTML 骨架 + 顶部引入 Supabase SDK（jsdelivr CDN）+ 按序引入 js 模块
css/style.css     全部样式（含响应式媒体查询）
js/core.js        常量/默认数据/状态与持久化/迁移/通用工具（DEFAULTS、load/save、deepMerge、migrateData）
js/sync.js        导入导出 + Supabase 同步（SupabaseClient、initSync、syncToSupabase、pullFromSupabase）
js/fitness.js     体重/围度/饮食/运动/热量平衡 五个健身模块
js/overview.js    概览页（问候/热量三卡/生理期预测/实时工资/运动/围度趋势/补货提醒）
js/salary.js      基本工资/房补/汇总 + 详情/新增子页面
js/settings.js    设置页（身体资料/记录模式/AI 配置/Supabase 同步/数据备份/危险区）
js/stock.js       囤货模块（统计/新增/全部/详情/空间 + 贴纸/扫码/识图）
js/passwords.js   密码管理
js/app.js         导航树/路由/渲染/底部 Tab/自定义选择器/启动（initSync + SW 注册）
seed-data.js      个人种子数据（生理期 87 条/基本工资 34 条/房补 43 条），暴露 window.__SEED_DATA__；
                  已加入 .gitignore，不上传 GitHub（隐私保护）
manifest.json     PWA 清单：display:standalone 全屏、theme/background #E6F7FF、icon 1024×1024
sw.js             Service Worker：PRECACHE 主页面+全部 js/css+清单+图标；主页面网络优先，其余缓存优先
icon.jpg / icon.svg  PWA 应用图标
supabase/functions/  Edge Functions（barcode-lookup、generate-sticker）
supabase/config.toml Supabase CLI 项目配置（含 verify_jwt 关闭）
supabase_migration.sql  初始化 SQL（user_data/user_config 表、RLS、storage 桶与策略）
.github/workflows/deploy-supabase.yml  push 后自动部署 Edge Functions 与函数 secrets
.gitignore        仅排除 seed-data.js 等个人文件
```
> 维护约定：改样式改 `css/style.css`，改某模块逻辑改对应 `js/*.js`，改导航/路由改 `js/app.js`。

### 3.2 运行时数据流
```
state (内存对象)
  └─ load() 从 localStorage 读取 → migrateData() 版本迁移（DATA_VERSION=4） → deepMerge 到 DEFAULTS
  │    （localStorage 为空时用 seed-data.js 兜底填充）
  │    启动后 initSync() 异步初始化 Supabase → 登录 → 拉取 → mergeRemote() 合并 → 写回本地
  └─ navigate(id) → PAGES[id]() 生成 HTML 字符串 → 注入 #page → bindPage(id) 绑定事件
  └─ save() 任意变更后写回 localStorage → 已配置 Supabase 时自动 syncToSupabase() 上传
```

### 3.3 数据模型（state）
```js
{
  meta:    { version: 4, createdAt, lastModified },
  profile: { weight: null, height: null },        // 用于运动热量估算
  periods: [ {start, end, note} ],                // 生理期记录（v4 起 end 必填）
  settings:{
    api:    { endpoint, key, model, enabled },    // 饮食AI识别配置（OpenAI 兼容）
    modules:{ weight:"cumulative", measure:"cumulative", diet:"daily", exercise:"daily", balance:"cumulative" },
    period: 90, dietGoal: 1600,                   // 健身默认周期 / 每日热量目标
    navOpen:{ fitness:false, period:false, salary:false },  // 分组展开状态
    supabaseUrl:"", supabaseKey:"", supabaseEmail:"", supabasePass:"",
    supabaseLastSync:null, supabaseLastSyncStatus:"local_only",
    supabaseDebug:"", supabasePendingSync:false,  // 离线待补传标记
    modelscopeToken:"", modelscopeModel:"iic/stable-diffusion-xl-base-1.0",  // AI 贴纸
    apizeroKey:"",                                // 条码查询（可选，配置后额度 200/日）
    passwordCategories: [...],                    // 密码分类列表
    spaces: { id:"root", name:"全部空间", children:[
      {id:"space_1", name:"客厅", children:[{id:"space_1_1", name:"冰箱", children:[]}]},
      {id:"space_2", name:"主卧", children:[]}
    ]}
  },
  weight:   [ {date, value, note} ],
  measure:  [ {date, waist, calf, arm, thigh, hip} ],
  diet:     [ {date, items:[ {name, kcal, baseKcal, meal, unit, qty, img, lastQty} ]} ],
  exercise: [ {date, items:[ {type, duration, kcal, note} ]} ],
  salary:   [ {date, category, 职务工资, ..., 实发工资} ],   // 36 个字段
  housingAllowance: [ {date, amount} ],
  stock:    [ {id, name, category, subcategory, quantity, unit,
               entryDate, productionDate, expiryDate, bestBeforeDate,
               storageLocation, status, icon, stickerUrl,   // 贴纸系统替代实物照片
               tags, minStock, consumption[], dismissedExpiry, notes} ],
  passwords: [ {id, site, username, password, category, note} ],
}
```

### 3.4 存储策略（本地持久化）
- 写入键 `fit_workbench_v1`，整对象 JSON 序列化。
- 读取用 `migrateData()`（`DATA_VERSION=4`）逐级迁移：v0→v1 囤货 `consumption`、v1→v2 囤货 `dismissedExpiry`、v2→v3 饮食 `meal`、v3→v4 经期 `end` 字段。
- JSON 导出：`身体管理数据_YYYYMMDD.json`；导入覆盖并二次确认。
- 云端备份与跨端同步详见「四」。

---

## 四、数据存储与多端同步（Supabase）

### 4.1 数据存储位置（三层 + 手动备份）
| 层级 | 位置 | 作用 |
|---|---|---|
| 一 | `localStorage`（键 `fit_workbench_v1`） | 运行时主存储，所有读写即时生效（本地优先） |
| 二 | `seed-data.js`（本地文件） | 个人种子数据兜底，`.gitignore` 不上传 GitHub |
| 三 | Supabase `user_data` 表 | 跨设备云同步 + 云端备份 |
| 附 | JSON 导出/导入 | 手动离线备份 |

### 4.2 Supabase 项目配置（已就绪）
- **Project URL**：`https://inhsftgjzkemsnwffvna.supabase.co`
- **数据表**：`user_data`（单表存整对象 JSONB，`user_id` 唯一索引支持 upsert，当前固定 `user_default`）、`user_config`（预留）。
- **Auth**：已启用 Supabase Auth，登录账号 `huangying0404@qq.com`；前端保存邮箱/密码到 `settings`（仅存本机），启动与同步前自动 `signInWithPassword`。
- **RLS**：已收紧 —— 仅 `auth.jwt() ->> 'email' = 'huangying0404@qq.com'` 可读写 `user_data`/`user_config`，其他注册用户也无法访问（`supabase_migration.sql` 含 `DROP POLICY IF EXISTS` 可重复执行）。
- **Storage 桶**：`stock-photos`（AI 识图临时图片）、`stock-stickers`（AI 生成贴纸），均为 public；写/删策略绑定登录邮箱。
- **Edge Functions**：`barcode-lookup`（条码查询代理）、`generate-sticker`（魔搭贴纸生成）。
- **配置方式**：设置页 → Supabase 云同步，填 Project URL / Anon Public Key / 登录邮箱 / 登录密码，保存后点「立即同步」。

### 4.3 同步机制（本地优先 + 自动补传）
- **触发时机**：
  1. 启动时 `initSync()` → `initSupabase()`（建客户端 + 自动登录）→ `pullFromSupabase()` → `mergeRemote()` 合并 → 写回本地并刷新页面；
  2. 任意 `save()` 后自动 `syncToSupabase()` 上传（静默，不弹 toast）；
  3. 设置页「立即同步」按钮手动触发。
- **合并策略**：`mergeRemote()` —— 本地为 0 条而云端有数据，或云端 `meta.lastModified` 更新时整体采用云端；始终保留本地 `settings`（token、邮箱密码等绝不上传，`getSyncData()` 剔除 `settings`）。
- **离线处理**：Supabase 未配置/未登录/SDK 未加载时，数据仅存本地并置 `supabasePendingSync = true`；下次启动 `initSync()` 检测到标记后自动补传。
- **状态回显**：`supabaseLastSync`（时间）、`supabaseLastSyncStatus`（ok / error:原因 / local_only）、`supabaseDebug`（滚动保留最近 20 行日志），设置页可见。
- **SDK**：`https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2`（工作台.html 头部引入）。

### 4.4 GitHub 仓库与自动部署
- **仓库**：`ginwolley/mymomo`（分支 `main`），GitHub Pages 自动托管前端。
- **本地 Git**：项目约定使用全路径 `C:\Program Files\Git\bin\git.exe`（避免 PATH 查找）。
- **推送流程**：修改代码 → `git add` → `git commit` → `git push origin main`；**改好代码后直接推送，无需询问**（项目约定）。
- **前端部署**：push 后 GitHub Pages 约 1–10 分钟生效；需立即查看可加 `?v=数字` 绕过缓存。
- **Edge Functions 自动部署**：`.github/workflows/deploy-supabase.yml` —— push 涉及 `supabase/functions/**` 时自动 `supabase functions deploy` 两个函数，并 `supabase secrets set` 写入函数环境变量。**以后改函数代码只需 push，无需手动部署**。
- **GitHub Secrets**（已配置）：`SUPABASE_ACCESS_TOKEN`、`SUPABASE_PROJECT_ID`（inhsftgjzkemsnwffvna）、`MODELSCOPE_TOKEN`、`APIZERO_KEY`。
- **不入库文件**：`seed-data.js`（.gitignore）、`debug.log`、demo/备份 HTML、设计文档、导出的数据 JSON 等。
- **PWA 更新**：Service Worker 注册带 `?v=时间戳` 强制检查；主页面走网络优先，保证每次打开为最新版本。

### 4.5 同步边界与隐私
- 上传内容仅限业务数据（periods / salary / housingAllowance / weight / measure / diet / exercise / stock / passwords / meta 等）；**Token、邮箱密码、偏好设置绝不上传**。
- 囤货已删除实物照片字段，改为贴纸系统（SVG/AI 图 URL），同步体积显著减小，规避超限问题。
- 密码等敏感字段以明文存在于 Supabase（RLS 已限制仅本人邮箱可访问）；重要数据建议另存 JSON 本地备份。
- 离线时同步暂停，恢复网络后由「保存」或「启动」自动补传。
- 登录邮箱/密码仅存本机 localStorage，不入库、不上传；新设备首次使用需在设置页填写后点「立即同步」。

---

## 五、导航系统

### 5.1 结构（`NAV_TREE`，桌面侧边栏）
```
概览
├─ 健身（可折叠分组）
│   ├ 体重
│   ├ 围度
│   ├ 饮食
│   ├ 运动
│   └ 热量平衡
├─ 生理期（叶子，直接进日历）
├─ 工资（可折叠分组）
│   ├ 基本工资
│   ├ 房补
│   └ 汇总
├─ 囤货（可折叠分组）
│   ├ 统计
│   ├ 新增物品
│   ├ 全部物品
│   └ 空间
├─ 密码（叶子）
└─ 设置
```

### 5.2 手机端底部 Tab（≤768px）
- 侧边栏隐藏，底部固定 Tab 栏：**首页 / 健康 / 囤货 / 更多**。
- `TAB_MATCH` 映射：首页=概览/工资/密码分段；健康=饮食/运动/体重/围度分段；囤货=统计/新增/全部/空间；更多=设置。
- 顶级功能页在手机端映射到对应 Tab 容器页（`pageTabHome`/`pageTabRecord`），保持分段条一致（`segHome`/`segRecord`）。
- **子页面**（工资详情/囤货详情/密码详情等）隐藏 Tab 栏并显示顶部返回按钮（`BACK_MAP` 映射）。
- 桌面端分组可折叠，展开状态记忆于 `navOpen`。

---

## 六、功能模块详述

### 6.1 概览（overview）
- **动态问候**：根据北京时间显示「早上好/中午好/下午好/晚上好，master」。
- **热量三卡**：今日净平衡 / 今日摄入 / 今日消耗（净 = 摄入 − 消耗），一行横向排列。
- **生理期预测卡**：若当前为经期，显示「生理期 第X天」；否则显示预计开始/结束 + 平均周期（简洁白卡 + 粉色标签样式）。
- **实时工资卡**：标题「今日已入账」+ 实时，根据当前时间（北京时间）每秒计算已赚金额。
- **今日运动卡**：今日运动记录列表；无记录时显示提示。
- **围度趋势卡**：近7天围度变化折线图，点击卡片跳转围度页。
- **补货提醒卡**：底部显示库存不足物品（数量 ≤ 最低库存）。
- 手机端首页 Tab 内，分段条位于 hero 卡正下方。

### 6.2 健身 · 体重（weight）
- 录入：日期 + 体重(kg) + 备注；同日记录自动覆盖更新。
- 趋势：周期内折线图（非柱状图）。
- 历史：周期内表格，可单条删除。
- 周期筛选器：近7天 / 近30天 / 近90天 / 全部（默认近90天）。
- 模式：每日 / 累计（设置切换）。

### 6.3 健身 · 围度（measure）
- 5 维度：腰围 / 小腿 / 上臂 / 大腿 / 臀围（cm）。
- 趋势：5 条彩色折线 + 图例。
- 历史：宽表，移动端横向滚动容器；可删除；受周期筛选控制。

### 6.4 健身 · 饮食（diet）
- **手动录入**：食物名称 + 热量(kcal)。
- **历史联想**：输入名称自动匹配历史记录，带出上次录入的重量（`lastQty`）和热量。
- **份量按钮**：「一人份(80g)」「小份(150g)」「大份(250g)」一行排列（`flex-wrap:nowrap`）。
- **包装食品计算**：独立区域，输入每100g千焦和重量，自动换算 kcal（`baseKcal` 上限 3000 防小分量换算爆炸）。
- **拍照识别（可选）**：调用户自有 OpenAI 兼容视觉接口；失败退回手动。
- 每日模式：今日列表 + 小计 + 「重置今日」；累计模式：周期历史 + 合计。

### 6.5 健身 · 运动（exercise）
- **热量估算（离线）**：`kcal = MET × 体重 × 时长(h)`。
- 运动方式：分类下拉（有氧/力量/瑜伽/舞蹈跟练/其他）带预设 MET，自定义 picker 面板；切换类型实时更新热量预览。
- 每日/累计 + 周期筛选同饮食。

### 6.6 健身 · 热量平衡（balance）
- 净平衡 = 摄入 − 消耗，按周期统计。
- 顶部三卡 + 每日摄入vs消耗折线对比图（折线，非柱状）。
- 历史表：日期、摄入、消耗、净值（正数青绿，负数粉红）。

### 6.7 生理期（period）— 重点模块
- **月历视图**：CSS Grid 7 列，经期日粉红填充，跨月日期淡化，今日描边；预测区间虚线。
- **翻查**：年/月下拉 + 上/下一月按钮，可查任意历史年月。
- **预测**：最近 6 次间隔与经期长度的**中位数**，计算下次开始/结束 + 倒计时；标准差判规律度。
- **统计卡**：平均周期、平均经期、预计开始、预计结束（一行四个白色块，玫瑰色文字）。
- **记录交互**：日历点选日期 → 「是」标记开始/结束 → 「否」取消标记。
- **历史记录**：默认显示条数，可「展开全部记录」进入新页面，支持删除。
- **全年视图**：多年度小日历纵向排列，当前年高亮，自动滚动到当前年。
- **预置数据**：87 条真实经期（2017-11 至 2026-07）。

### 6.8 工资 · 基本工资（salary-basic）
- **录入**：36 个字段（职务工资、级别工资、岗位工资、住房公积金、实发工资等），日期格式 `YYYY.M`。
- **录入方式**：「记录基本工资」按钮进入新页面，所有输入框为纯文本输入。
- **查看/编辑**：查看按钮进详情页（两列布局），可修改或删除。
- **列表**：默认近 12 个月，超过有「展开全部记录」。
- **按钮主题**：青绿主题（`.btn.primary`，非金色）；**不显示时薪信息**。
- **种子数据**：预置 2022.12–2026.08 共 34 条。

### 6.9 工资 · 房补（salary-housing）
- 录入：日期 + 金额，纯文本输入；查看/编辑/删除同基本工资。
- **种子数据**：预置 2023.01–2026.07 共 43 条。

### 6.10 工资 · 汇总（salary-summary）
- **统计卡片**：总应发、总实发、总房补、总公积金 + 总包（实发+房补+公积金，大号居中）。
- **年度表格**：按年度分组降序，列：年度、应发、实发、房补、公积金、总包（加粗）。
- 数值居中，表头和数据对齐一致。

### 6.11 囤货 · 统计（stock-overview）
- **顶部统计卡**：总物品数、未开封、使用中、已用完。
- **按分类统计** + **库存不足提醒**（红色左边框）+ **即将过期提醒**（30天内，可手动忽略 ×）+ **最近消耗记录**（5条）。
- 快捷入口：新增物品、录入消耗。

### 6.12 囤货 · 新增物品（stock-add）
- 字段：名称、分类、子分类、数量、单位、录入日期、生产日期、保质期至、最佳食用日期（仅食品）、空间、状态、标签、最低库存、备注。
- **贴纸系统**：内置 26 类白边手绘 SVG 贴纸（按名称关键词自动匹配）+ emoji 库兜底；AI 专属贴纸生成后展示（见 9.5）。
- **扫码录入**：按钮触发 → 原生 `BarcodeDetector`（EAN/UPC/CODE/QR）→ 无则加载 `html5-qrcode` 降级 → 扫到条码后经 `barcode-lookup` Edge Function 查询 → 自动置入商品名/分类/单位（`inferStockCategory` 关键词推断）。
- **AI识图**：拍照 → 压缩 → 优先经 Edge Function 代理识别（图片传 Storage 后由服务端调用视觉接口）→ 失败降级直连用户配置的接口 → 自动填名称/分类。识别用图不保存到物品。

### 6.13 囤货 · 全部物品（stock-all）
- **卡片展示**：按空间分组，每项显示贴纸、名称、状态标签、分类、数量、过期日期、标签。
- **筛选**：搜索、分类、状态、一级空间（级联）、二级空间（动态）、标签。
- **操作**：查看（详情页）、删除。低库存红边 + 「补货」标签。

### 6.14 囤货 · 物品详情（stock-detail）
- 编辑所有字段，保存修改或删除；消耗记录列表（只读）。

### 6.15 囤货 · 空间管理（stock-spaces）
- **树形结构**：递归渲染，支持无限层级；连接线 + 颜色左边框 + 不同 SVG 图标。
- **操作**：新增子空间（+）、删除空间（×，二次确认）、添加一级空间。
- 每个空间卡片显示物品数量。默认空间：客厅 > 冰箱、主卧。
- **注**：冰箱「视图」页已删除，冰箱仅作为默认空间示例存在。

### 6.16 密码管理（passwords）
- **分类管理**：自定义分类（工作/社交/银行/购物/娱乐/其他），拖拽排序。
- **列表**：按分类分组，仅显示网站名称，点击进详情页；搜索按名称。
- **详情**：网站名称、用户名、密码（默认隐藏）、备注、分类；密码行右对齐 eye 图标（显示/隐藏）+ copy 图标，用户名/网址行有 copy 图标。

### 6.17 设置（settings）
- **身体资料**：默认体重/身高。
- **记录模式**：5 模块「每日/累计」切换（按钮宽布局 `max-width:260px`）。
- **饮食热量目标**：每日 kcal 目标（默认 1600）。
- **AI 拍照识别（饮食）**：启用开关 + 接口地址 + 模型名 + API Key（仅存本机）。
- **囤货 AI 贴纸与条码查询**：魔搭访问令牌（图片生成，经 Edge Function 转发不暴露前端）+ 魔搭图片模型 + 条码查询 API Key（apizero，可选，配置后额度 20→200/日）。
- **Supabase 云同步**：Project URL / Anon Public Key / 登录邮箱 / 登录密码 + 「保存配置」+「立即同步」+ 同步状态 + 调试日志。
- **生理期数据**：记录数 + 清空按钮。
- **数据备份**：导出 / 导入 JSON。
- **危险区**：清空全部数据（保留设置，二次确认）。

---

## 七、生理期核心算法

### 7.1 统计（`periodStats`）
- 周期集合：相邻两次开始日间隔；经期长度：每次 `结束 − 开始 + 1`。
- 输出：平均周期、平均经期、周期标准差（≤3 较规律，≤7 一般，>7 不规律）、上次开始日。
- 预测基准：**最近 6 次的中位数**（对不规律周期更稳健）。

### 7.2 预测（`predictNext`）
```
start = 上次开始日 + 中位周期
end   = start + 中位经期长度 − 1
```
倒计时：「距预计开始还有 N 天」/「预测经期进行中」/「已超出，请记录实际日期」。

### 7.3 命中判定（`periodAt`）
某日期落在 `[start, end]` 闭区间内即视为经期。

---

## 八、实时计时器（今日已入账）

- `pageOverview()` 初始化，`startSalaryTimer()` 每秒更新 `#todayEarned`；切换页面 `stopSalaryTimer()` 停止。
- 公式：日薪 = (当月实发 + 当月房补) ÷ 工作日数；时薪 = 日薪 ÷ 6；工作时间段 9:00-11:30、14:00-17:30；按已工作小时累加，17:30 后计满 6 小时。

---

## 九、囤货模块核心逻辑

### 9.1 分类体系
| 一级分类 | 子分类示例 | 默认单位 |
|---|---|---|
| 化妆品 | 粉底液、散粉、定妆喷雾、睫毛膏、眼线笔等 17 种 | 个 |
| 护肤品 | 洗面奶、洗发水、发膜、身体乳、精华等 12 种 | 瓶 |
| 日化用品 | 抽纸、卷纸、洗洁精、洗衣液、垃圾袋等 13 种 | 包 |
| 食品 | 鲜菜、水果、肉类、牛奶、面包、速食产品等 11 种 | kg |
| 其他 | 其他 | 个 |

### 9.2 分类颜色与图标
- 每分类映射背景色/边框色/文字色 + emoji 兜底图标（`getCategoryColor`、`STOCK_EMOJIS`）。

### 9.3 贴纸系统（替代实物照片）
优先级：**AI 专属贴纸（`item.stickerUrl`）> 内置白边手绘贴纸（`STOCK_STICKERS`，26 类按名称关键词匹配）> emoji 兜底**。
- 内置贴纸为手绘风 SVG（白底深描边），免费即时、零 API 消耗，覆盖饮料/牛奶/面包/水果/鸡蛋/米面/方便面/零食/肉/蔬菜/日化/护肤/彩妆/药品/工具等。
- AI 专属贴纸：新增/编辑物品时 `autoGenerateSticker()` 调用 `generate-sticker` Edge Function → 魔搭 text2image 生成 → 上传 `stock-stickers` 桶 → 返回 public URL 缓存于 `item.stickerUrl`；未配置魔搭 token 或失败则静默跳过。

### 9.4 扫码自动置入
- `lookupBarcode()`：优先 `barcode-lookup` Edge Function（Key 在服务端）→ 失败降级直连 apizero（`https://v1.apizero.cn/api/barcode-lookup`）。
- 扫到条码后自动填充名称（`扫码: 条码` → 查询到商品名则替换）、分类、单位、数量默认值。

### 9.5 空间管理
- 树形结构，无限层级；新增/删除空间用自定义弹窗（`showInputDialog`，替代 `prompt()`）；删除二次确认。

### 9.6 消耗与过期提醒
- 统计页「录入消耗」弹窗：选物品、数量、日期、备注；自动累计，消耗完自动标「已用完」。
- 30 天内过期物品统计页提醒（食品同时查保质期至与最佳食用日期），可忽略（`dismissedExpiry`）。

---

## 十、移动端适配要点（vivo X200 Pro Mini / 夸克）

- 侧边栏 → 底部 Tab 栏（首页/健康/囤货/更多），子页面隐藏 Tab 显示返回按钮。
- 表单字段手机端单列铺满，按钮放大；输入字号 16px 防聚焦缩放。
- 宽表格（围度 6 列）横向滚动容器。
- 日历窄屏缩小字号圆角；统计卡片数值缩小允许换行。
- 内容区 `max-width:100%`；`env(safe-area-inset-bottom)` 适配底部安全区。
- 隐藏右侧滚动条（保留滚动）。
- **状态栏**：`theme-color: #E6F7FF` + `default` 模式（勿用 black-translucent，否则 Android 透出渐变导致色差），body/topbar 纯色与状态栏一致。

---

## 十一、密码模块核心逻辑

- 数据：`{id, site, username, password, category, note}`；分类拖拽排序（HTML5 DnD）。
- 密码默认隐藏，eye 图标切换；复制图标独立；数据本地存储，云同步经 Supabase（RLS 绑定本人邮箱）。

---

## 十二、已知限制与边界

1. **localStorage 持久化**：个别机型 `file://` 下有限制，定期导出 JSON 为保险。
2. **饮食 AI 识别 CORS**：直连部分接口可能跨域拦截；失败不影响手动。
3. **预测仅供参考**：基于历史中位数，身体实际为准。
4. **扫码依赖相机权限**：原生 `BarcodeDetector` 仅 Chrome/Edge 支持；降级 `html5-qrcode`（CDN 按需加载）。
5. **安卓状态栏**：部分手机系统（vivo OriginOS）对状态栏叠加半透明遮罩，可能有轻微色差，属系统级行为。
6. **同步依赖网络**：离线时数据仅存本地；恢复后由保存/启动自动补传（`pendingSync`）。
7. **Supabase 明文存储**：业务数据（含密码字段）明文存于 Supabase，RLS 已限制仅本人邮箱可读；重要数据另存 JSON 备份。
8. **recognize-product Edge Function 未随仓库发布**：前端识图优先调用该函数（识别用图传 Storage 后服务端代理），仓库当前仅含 barcode-lookup / generate-sticker；函数缺失时前端自动降级为直连用户配置的视觉接口。
9. **PWA 首次安装需网络**：首次打开需联网，之后 SW 缓存支持离线。
10. **GitHub Pages 缓存**：推送后 1–10 分钟生效，加 `?v=数字` 可绕过。
11. **Supabase Auth 登录**：未在设置页填写邮箱/密码时无法同步（RLS 仅 authenticated 可读写）；填错则同步报错并在设置页回显原因。

---

## 十三、需求映射表

| 用户需求 | 落地方案 | 状态 |
|---|---|---|
| 工作台含运动/体重/围度/饮食/运动热量 | 健身分组 5 模块 + 概览 | ✅ |
| 数据本地留存 + 多端同步 | localStorage + Supabase（启动拉取/保存上传/手动同步） | ✅ |
| 手机打开使用 | GitHub Pages + 底部 Tab + 响应式 | ✅ |
| 手机全屏无浏览器栏 | PWA manifest `display:standalone` | ✅ |
| 离线可用 | Service Worker 缓存 + 本地优先 | ✅ |
| 个人种子数据不上传 GitHub | `.gitignore` 排除 `seed-data.js` | ✅ |
| 改好代码直接推送 | Git 全路径约定 + 自动 push | ✅ |
| 每日/累计记录模式 | 每模块独立切换 | ✅ |
| 生理期月历 + 预测 + 全年视图 | 中位算法 + 倒计时 + 多年度纵排 | ✅ |
| 工资记录管理（不含时薪展示） | 基本工资 + 房补 + 汇总，青绿主题 | ✅ |
| 囤货物品管理 | 卡片 + 空间分组 + 筛选 | ✅ |
| 扫码自动置入商品信息 | BarcodeDetector + apizero Edge Function + 分类推断 | ✅ |
| AI识图录入 | Edge Function 代理（图片传 Storage）→ 降级直连 | ✅ |
| 实物图片功能删除 | 已删，改贴纸系统 | ✅ |
| 冰箱视图删除 | 已删（冰箱保留为默认空间示例） | ✅ |
| AI 专属贴纸 | 内置 26 手绘 SVG + 魔搭 AI 生成兜底（经 Edge Function） | ✅ |
| 过期/补货提醒 | 30 天过期提醒 + 低库存提醒（概览+统计页） | ✅ |
| 实时工资显示 | 概览「今日已入账」每秒更新 | ✅ |
| 密码管理 | 分类 + 增删改查 + 显示切换 + 一键复制 | ✅ |
| 历史食物带出上次重量 | `buildFoodHistory()` 记录 `lastQty` | ✅ |
| 围度趋势首页展示 | 概览围度趋势卡 + 折线图，点击跳转 | ✅ |
| 动态问候 | 北京时间 早上/中午/下午/晚上 | ✅ |
| 沉浸式布局 | 状态栏/顶栏/内容区色号统一 | ✅ |
| Supabase 安全加固 | Auth 登录 + RLS 绑定本人邮箱 + Storage 策略 | ✅ |
| Edge Functions 自动部署 | GitHub Actions + 4 个仓库 Secrets（已配置） | ✅ |
| 单文件拆分多模块 | 9 个 js 模块 + css 独立，零依赖 | ✅ |

---

## 十四、后续扩展规划（架构已预留）

- 在 `NAV_TREE` 增加新分组/叶子即可挂载新板块。
- 运动类型 / 围度维度可在 `EX_TYPES` / `MEAS_KEYS` 增删。
- 如需更稳妥持久化可加 IndexedDB。
- 消耗记录撤销；购物清单；物品复制；按标签统计。
- 同步冲突升级为记录级合并（当前整体覆盖）。
- 敏感字段（密码/工资）本地加密后入库，云端密文存储。
- `recognize-product` Edge Function 补全并随仓库部署（当前降级直连）。
- 私有仓库整库备份（业务数据 JSON 定期 commit 到私有仓）。
- 长列表（囤货/密码）分页或「展示20条+展开全部」优化。

---

*文档基于 `工作台.html` 及 `js/` 各模块实际代码梳理，非凭记忆生成。*
